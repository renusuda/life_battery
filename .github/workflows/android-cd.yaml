name: Android CD

on: workflow_dispatch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter using FVM
        uses: kuhnroyal/flutter-fvm-config-action/setup@v3

      - name: Fetches and resolves project dependencies
        run: flutter pub get

      - name: Create key.jks
        run: |
          echo '${{ secrets.KEYSTORE_BASE64 }}' | base64 -d > android/app/release.jks

      - name: Create key.properties
        run: |
          echo 'storePassword=${{ secrets.ANDROID_STORE_PASSWORD }}' > android/app/key.properties
          echo 'keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}' >> android/app/key.properties
          echo 'keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}' >> android/app/key.properties
          echo 'storeFile=android/app/release.jks' >> android/app/key.properties

      - name: Build app bundle
        run: flutter build appbundle
